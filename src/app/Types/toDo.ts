export interface addListsType{
   _id: string,
    title: string | any,
    Editing?: boolean,
    Status?: boolean,
    CanEdit?: boolean,
    buildDate: number | any,
    updataDate: number | any,
}
